
require '../DB/connect.php';
          $result = mysqli_query($con, "SELECT colorcode FROM color");

          if ($result) {
              $count = 1
            while ($row = mysqli_fetch_array($result)) {
                $"$count" = $row['colorcode'];
                $count = $count +1;
            }
          }

if $stat == "waiting"{
    return 
}
elseif $stat =='working'{
    return
}